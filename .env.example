# BoardGameGeek Authentication
# Copy this file to .env.local and use ONE of the options below:

# OPTION 1: Session Cookie (quick, temporary)
# 1. Log into boardgamegeek.com in your browser
# 2. Open DevTools (F12) → Application → Cookies → boardgamegeek.com
# 3. Copy the value of "SessionID" cookie (or copy all cookies from Network tab)
BGG_COOKIE=SessionID=your_session_id_here

# OPTION 2: API Token (permanent, requires app registration)
# 1. Register at: https://boardgamegeek.com/applications
# 2. Once approved, click "Tokens" to generate a token
# BGG_API_TOKEN=your_token_here
